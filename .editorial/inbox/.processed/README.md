# Processed Inbox Items

This folder holds local ingress files that have already been processed.

Use it for:

- promoted prose drops
- mapped-but-held interpretation files
- processed images or sidecar assets

Rules:

- do not treat this folder as active inbox
- keep only what is still useful locally
- if a processed item no longer serves a purpose, remove it instead of treating `.processed/` as permanent archive
